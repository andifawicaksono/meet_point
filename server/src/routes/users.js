const { Router } = require('express');

const router = Router();

// Placeholder — controllers implemented in Phase 2
router.get('/', (_req, res) => res.json({ message: 'users routes — coming in Phase 2' }));

module.exports = router;
